/** @format */

import { FunctionComponent } from 'react';

const MaintainerIndex: FunctionComponent = () => {
    return (
        <div className='text-center'>
            <p>{`Are you trying to access the maintainer's console? 
            The console is in the 'account' page accessible on the header.`}</p>
        </div>
    );
};

export default MaintainerIndex;
